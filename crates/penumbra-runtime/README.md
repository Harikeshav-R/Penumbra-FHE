# penumbra-runtime

FHE runtime and primitive operations for Penumbra.

This crate is the only part of Penumbra that directly wraps `tfhe-rs`. It provides the cryptographic primitives, key generation, and the engine for executing compiled graphs.
