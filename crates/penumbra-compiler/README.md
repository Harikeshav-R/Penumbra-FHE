# penumbra-compiler

Lowering passes and polynomial approximations for Penumbra.

This crate takes the `penumbra-ir` graph and compiles it into an executable graph against the `penumbra-runtime`. It handles polynomial approximations and quantization steps.
