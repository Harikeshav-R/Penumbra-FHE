# Benchmarks

This directory contains performance benchmarks for the Penumbra FHE engine.
We use `criterion` for Rust-side microbenchmarks and `pytest-benchmark` for end-to-end Python performance tests.
